# Python
__pycache__/
*.py[cod]
*.pyo
.Python
*.egg-info/
dist/
build/
.eggs/
.venv/
venv/

# Data (large files, never commit). Anchored patterns only — a bare "data/"
# also matches CODE subpackages like src/qymyz_embed/data/ and made hatchling
# drop them from wheels (Kaggle failure 2026-07-05).
/data/
embed/data/
evallab/data/
outputs/
checkpoints/
evals/
wandb/
*.bin
*.npy
*.model
*.vocab

# Hydra outputs
outputs/
multirun/

# IDE
.idea/
.vscode/
*.swp

# uv
.uv/

# OS
.DS_Store
Thumbs.db

# Papers (reference PDFs, too large for git)
papers/

# Session files
session.txt

# Tool caches
.pytest_cache/
.ruff_cache/

# Claude Code memory (private)
.claude/

# private ledgers / state — never track (CLAIM.md is the private claims convention)
CLAIM.md
SESSION.md

# secrets — never track
.env
*.env
!.env.example
